version: 2

sphinx:
  configuration: source/conf.py

submodules:
  include: all

build:
  image: latest

python:
  version: 3.7
  install:
    - requirements: requirements.txt
  system_packages: true

formats: [htmlzip, pdf]
